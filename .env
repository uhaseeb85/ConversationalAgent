# AI Configuration (defaults - override in Settings UI)
# For LM Studio (local):
VITE_AI_BASE_URL=http://localhost:1234/v1
VITE_AI_API_KEY=lmstudio
VITE_AI_MODEL=local-model

# For OpenRouter:
# VITE_AI_BASE_URL=https://openrouter.ai/api/v1
# VITE_AI_API_KEY=sk-or-your-key-here
# VITE_AI_MODEL=openai/gpt-4o-mini

# ─── Auth & Server ────────────────────────────────────────────────────────────

# JWT signing secret — change this before deploying to production!
JWT_SECRET=change-me-in-production-use-a-long-random-string

# First admin account — seeded on first startup if no users exist
FIRST_ADMIN_EMAIL=admin@example.com
FIRST_ADMIN_PASSWORD=changeme123

# Application database (stores users, flows, submissions, execution history)
# Options: "sqlite" (default) or "postgres"
APP_DB_TYPE=sqlite
APP_DB_PATH=./app.db

# For Postgres app DB (used when APP_DB_TYPE=postgres):
# APP_DATABASE_URL=postgresql://user:password@localhost:5432/conversational_agent

# CORS allowed origin for the frontend (used in production)
ALLOWED_ORIGIN=http://localhost:5173

# ─── Email / SMTP (optional — for password reset emails) ─────────────────────
# If not set, reset tokens are printed to the server console (dev mode).
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=noreply@example.com
# SMTP_PASS=your-smtp-password
# SMTP_FROM="WorkFlow Studio <noreply@example.com>"
# APP_URL=http://localhost:5173
