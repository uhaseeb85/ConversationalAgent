# ═══════════════════════════════════════════════════════════════════════════════
# Conversational Onboarding - Environment Configuration
# ═══════════════════════════════════════════════════════════════════════════════

# ──── AI Configuration (Client-side) ─────────────────────────────────────────
# These are defaults - users can override in Settings UI
# For LM Studio (local):
VITE_AI_BASE_URL=http://localhost:1234/v1
VITE_AI_API_KEY=lmstudio
VITE_AI_MODEL=local-model

# For OpenRouter:
# VITE_AI_BASE_URL=https://openrouter.ai/api/v1
# VITE_AI_API_KEY=sk-or-your-key-here
# VITE_AI_MODEL=openai/gpt-4o-mini

# ──── Authentication & Security ──────────────────────────────────────────────
# 🔒 CRITICAL: Change this to a strong random string in production!
# Generate with: openssl rand -base64 32
JWT_SECRET=dev-secret-change-in-production

# 🔒 REQUIRED in production: Set to your frontend URL
# Example: https://app.yourdomain.com
ALLOWED_ORIGIN=

# Node environment (set to 'production' for secure cookies)
NODE_ENV=development

# Server port
PORT=3001

# ──── Application Database ───────────────────────────────────────────────────
# Stores users, flows, submissions, execution history
# Options: "sqlite" (default, single file) or "postgresql"
APP_DB_TYPE=sqlite

# SQLite database file path (when APP_DB_TYPE=sqlite)
APP_DB_PATH=./app.db

# PostgreSQL connection string (when APP_DB_TYPE=postgresql)
# Example: postgresql://user:password@localhost:5432/conversational_agent
# APP_DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# ──── First Admin Account ────────────────────────────────────────────────────
# Seeded on first startup if no users exist
# 🔒 IMPORTANT: Change the password after first login!
FIRST_ADMIN_EMAIL=admin@example.com
FIRST_ADMIN_PASSWORD=changeme123
FIRST_ADMIN_NAME=Admin

# ──── Email / SMTP (Optional) ────────────────────────────────────────────────
# Required for password reset emails
# If not configured, reset tokens are printed to server console (dev mode only)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=noreply@example.com
# SMTP_PASS=your-smtp-password
# SMTP_FROM="WorkFlow Studio <noreply@example.com>"

# Application URL (used in password reset email links)
# APP_URL=http://localhost:5173

# ═══════════════════════════════════════════════════════════════════════════════
# PRODUCTION DEPLOYMENT CHECKLIST
# ═══════════════════════════════════════════════════════════════════════════════
# 
# ✅ Set NODE_ENV=production
# ✅ Change JWT_SECRET to a strong random string
# ✅ Set ALLOWED_ORIGIN to your frontend domain
# ✅ Use PostgreSQL (APP_DB_TYPE=postgresql) with APP_DATABASE_URL
# ✅ Configure SMTP for password reset emails
# ✅ Change FIRST_ADMIN_PASSWORD and rotate after first login
# ✅ Use HTTPS/TLS for all connections
# ✅ Ensure target databases use restricted user accounts (not admin/root)
# ✅ Enable database connection encryption (SSL/TLS)
# ✅ Set up regular backups of the app database
# ✅ Configure firewall rules to restrict database access
# ✅ Review and monitor execution_history table for suspicious SQL
#
